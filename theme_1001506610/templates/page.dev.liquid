<section class="page-dev">
  <div class="page-dev__inner container">
    <h1 class="page-dev__title">{{ page.title }}</h1>

    {%- if page.content != blank -%}
      <div class="page-dev__content">
        {{ page.content }}
      </div>
    {%- endif -%}
  </div>
</section>
