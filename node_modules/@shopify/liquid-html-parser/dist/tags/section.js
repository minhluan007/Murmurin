"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.sectionTag = void 0;
const tokenizer_1 = require("../markup/tokenizer");
const types_1 = require("../types");
const tag_definitions_1 = require("../tag-definitions");
exports.sectionTag = {
    kind: tag_definitions_1.TagKind.Hybrid,
    parse(_name, markup, _parser) {
        const name = markup.valueExpression();
        if (name.type !== types_1.NodeTypes.String) {
            throw new Error(`Expected string for section name, got ${name.type}`);
        }
        const args = [];
        if (markup.consumeOptional(tokenizer_1.MarkupTokenType.Comma)) {
            args.push(...markup.blockNamedArguments());
        }
        return {
            type: types_1.NodeTypes.SectionMarkup,
            name,
            args,
            position: { start: name.position.start, end: markup.peek().start },
            source: name.source,
        };
    },
};
//# sourceMappingURL=section.js.map