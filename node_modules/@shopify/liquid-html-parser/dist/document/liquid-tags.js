"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.parseLiquidTag = parseLiquidTag;
const tokenizer_1 = require("./tokenizer");
const factories_1 = require("./factories");
const environment_1 = require("../environment");
const errors_1 = require("../errors");
const utils_1 = require("../utils");
const parser_1 = require("../markup/parser");
const tokenizer_2 = require("../markup/tokenizer");
const liquid_blocks_1 = require("./liquid-blocks");
const liquid_raw_1 = require("./liquid-raw");
const liquid_hybrid_1 = require("./liquid-hybrid");
// liquidTag := "{%" tagName markup "%}" (block | raw | hybrid)?
function parseLiquidTag(parser) {
    const openToken = parser.consume(tokenizer_1.TokenType.LiquidTagOpen);
    parser.accept(tokenizer_1.TokenType.Text);
    const closeToken = parser.consume(tokenizer_1.TokenType.LiquidTagClose);
    const envelope = (0, factories_1.envelopeFromTokens)(openToken, closeToken, parser.getSource());
    if (envelope.tagName.startsWith('#')) {
        const commentMarkup = envelope.tagName.slice(1) + envelope.markupString;
        const commentMarkupOffset = envelope.markupEnd - commentMarkup.length;
        return (0, factories_1.makeLiquidTagBaseCase)({
            ...envelope,
            tagName: '#',
            markupString: commentMarkup,
            markupOffset: commentMarkupOffset,
        });
    }
    if (envelope.tagName.startsWith('end')) {
        const innerName = envelope.tagName.slice(3);
        const innerDef = parser.tagForName(innerName);
        if (innerDef) {
            throw new errors_1.LiquidHTMLASTParsingError(`Attempting to close LiquidTag '${innerName}' before it was opened without a matching '${innerName}'`, parser.getSource(), openToken.start, closeToken.end);
        }
    }
    const def = parser.tagForName(envelope.tagName);
    if (!def) {
        return (0, factories_1.makeLiquidTagBaseCase)(envelope);
    }
    switch (def.kind) {
        case environment_1.TagKind.Tag:
            return parseStandaloneTag(parser, def, envelope, closeToken);
        case environment_1.TagKind.Block:
            return (0, liquid_blocks_1.parseBlockTag)(parser, def, envelope, closeToken);
        case environment_1.TagKind.Raw:
            return (0, liquid_raw_1.parseRawTag)(parser, def, envelope, closeToken);
        case environment_1.TagKind.Hybrid:
            return (0, liquid_hybrid_1.parseHybridTag)(parser, def, envelope, closeToken);
        default:
            return (0, utils_1.assertNever)(def);
    }
}
// standaloneTag := "{%" tagName markup "%}"
function parseStandaloneTag(parser, def, envelope, closeToken) {
    let reason;
    try {
        const markupStringStart = closeToken.start - envelope.markupString.length;
        const markupStringEnd = markupStringStart + envelope.markupString.length;
        const tokens = (0, tokenizer_2.tokenizeMarkup)(envelope.markupString, markupStringStart);
        const markupParser = new parser_1.MarkupParser(tokens, parser.getSource(), markupStringStart, markupStringEnd);
        if (parser.isTolerant())
            markupParser.enableTolerant();
        const markup = def.parse(envelope.tagName, markupParser, parser);
        if (!markupParser.isAtEnd()) {
            return (0, factories_1.makeLiquidTagBaseCase)(envelope, undefined, undefined, undefined, 'unexpected tokens after markup');
        }
        return (0, factories_1.makeLiquidTagNamed)(envelope, markup);
    }
    catch (e) {
        reason = e instanceof Error ? e.message : 'unknown error';
        return (0, factories_1.makeLiquidTagBaseCase)(envelope, undefined, undefined, undefined, reason);
    }
}
//# sourceMappingURL=liquid-tags.js.map